# **Лабораторна робота №1**

## **Варіант 3**

**Виконала: Успенська Емілія**

**Тема**: Створення першого проєкту. Класи, об’єкти, конструктори та деструктори.

**Мета**: Ознайомитися з середовищем розробки, навчитися створювати класи та працювати з базовими елементами ООП у C#. Налаштувати середовище розробки.

## **Хід роботи**

1. Встановлено Visual Studio Code та необхідні розширення (C# Dev Kit, .NET Install Tool)

2. Створено консольний проєкт `lab1v3` за допомогою команди `dotnet new console -o OOP-Uspenska/lab1v3`

3. У файлі `Program.cs` реалізовано клас `Student` згідно з варіантом 3:
   - приватні поля `name`, `id`, `averageMark`;
   - публічні властивості `Name`, `Id`, `AverageMark` (з перевіркою діапазону 0–100 для середнього балу);
   - конструктор, що ініціалізує об'єкт;
   - метод `PrintCard()`, який виводить дані студентського квитка в консоль.

4. У методі `Main` створено 3 об'єкти класу `Student`, викликано метод `PrintCard()` для кожного з них

5. Створено `.gitignore` у кореневій директорії репозиторію (згенеровано через toptal.com для VS Code, Visual Studio, C#, .NET Core)

6. Ініціалізовано Git-репозиторій, зроблено коміти з описом змін та відправлено код у віддалений репозиторій на GitHub

## Результат роботи

<img width="1179" height="550" alt="photo_1_2026-09-18_14-44-50" src="https://github.com/user-attachments/assets/964669a6-9695-4cbd-9e30-9c6ce1208cf4" />

## **Контрольні запитання**
**1. Що таке клас та об’єкт? Яка між ними різниця?**

клас - це креслення або шаблон, за яким створюються об’єкти. об'єкт це конкретний екземпляр (представник) класу. якщо клас студент - це шаблон, то об’єкти - це конкретні студенти: Іван Петренко, Марія Сидоренко. Кожен об’єкт має свої власні значення полів.

**2. Для чого використовуються конструктори? Чи може клас існувати без них?**

конструктор використовується для початкової ініціалізації даних об'єкта під час створення через new. варіанти конструкторів можуть бути такими: за замовчуванням, параметризований, та перевантаження конструкторів

так, я думаю, що клас може існувати без явно написаного конструктора тому що, у такому разі C# автоматично створює стандартний конструктор за замовчуванням.

**3. Чим відрізняється приватне поле від публічної властивості? Навіщо потрібна інкапсуляція?**

приватне поле (private) зберігає дані всередині класу і недоступне ззовні, а публічна властивість (public) надає контрольований доступ до цього поля через блоки get та set. Інкапсуляція потрібна для приховування внутрішньої реалізації та захисту даних від некоректного змінення ззовні.




